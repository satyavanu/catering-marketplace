version: 1
applications:
  - appRoot: apps/web
    buildPath: /
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
            - npm dedupe
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: apps/web/.next
        files:
          - '**/*'